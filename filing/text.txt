1. 
FILE *fp;
fp = fopen("student.txt", "w");

2.  c

3.  b, fclose()

4. b, End of file

5. #include <stdio.h>

int main() {
    FILE *fp;

    fp = fopen("data.txt", "w");

    fprintf(fp, "Hello");

    fclose(fp);

    return 0;
}

6. Pakistan

7. false, it wipes all the data

8. Ali 20

9. fscanf()

10. you dont do "r", when writing in a file, do "w" instead

11. c, "a"

12.   fp = fopen("data.txt", "r");

  if (fp == NULL) {
      printf("File cannot be opened");
  }

13. #include <stdio.h>

int main(){
  char message[99];
  FILE *fp;
  fp = fopen("message.txt", "w");

  fprintf(fp, "Good Luck for Quiz!");
  fscanf(fp, "%s", message);
  printf("%s", message);
  
  fclose(fp);

  return 0;
}
why is this wrong??

14. #include <stdio.h>

int main(){
  int num;

  FILE *fp;

  fp = fopen("number.txt", "r");

  fscanf(fp, "%d", &num);

  printf("Number stored is: %d", num);
  fclose(fp);

  return 0;
}

15. ABC